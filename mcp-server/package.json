{
  "name": "13flow-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "13FLOW MCP server over Streamable HTTP, with public read-only tools and x402-gated Pro tools.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check test-client.mjs",
    "test": "node test-client.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "zod": "^3.25.0"
  }
}
